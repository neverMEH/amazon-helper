# Invalid guide for testing error handling
guide:
  # Missing required 'name' field
  category: "Test Category"
  # Missing required 'short_description' field
  difficulty_level: "invalid_level"  # Invalid value
  estimated_time_minutes: "not_a_number"  # Wrong type

sections:
  - section_id: "test1"
    title: "Test Section 1"
    display_order: 1
  - section_id: "test1"  # Duplicate section_id
    title: "Test Section 2"
    display_order: 2

queries:
  - title: "Test Query"
    # Missing required 'sql_query' field
    display_order: 1
    query_type: "invalid_type"  # Invalid query type
    parameters_schema:
      param1:
        # Missing required 'type' field
        description: "Test parameter"

metrics:
  - metric_name: "test_metric"
    display_name: "Test Metric"
    definition: "Test definition"
    metric_type: "invalid_type"  # Invalid metric type
  - metric_name: "test_metric"  # Duplicate metric name
    display_name: "Another Test Metric"
    definition: "Another definition"
