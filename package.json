{
  "name": "amc-manager",
  "version": "1.0.0",
  "description": "AMC Manager Application",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "python main_supabase.py"
  }
}